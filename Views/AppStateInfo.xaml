<UserControl x:Class="AOEMatchDataProvider.Views.AppStateInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AOEMatchDataProvider.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:common="clr-namespace:AOEMatchDataProvider.Controls.Common"
             mc:Ignorable="d" 
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="250" 
             d:DesignWidth="500"
             >
    <Grid Background="{DynamicResource BackgroundBrush}">
        <!--<WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
            <Label Foreground="Wheat">b</Label>
            <common:KeyHint></common:KeyHint>
            <Label Foreground="Wheat">b</Label>
        </WrapPanel>-->
        <TextBlock Text="{Binding Description, FallbackValue='Invalid Binding'}" FontSize="16pt" Foreground="{DynamicResource ForegroundBrush}" VerticalAlignment="Center" TextAlignment="Center" Padding="32"/>
    </Grid>
</UserControl>
